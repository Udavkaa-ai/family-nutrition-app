const { Router } = require('express');

const router = Router();

// POST /api/recipes/generate
// GET  /api/recipes/:familyId
// GET  /api/recipes/:familyId/:recipeId
// DELETE /api/recipes/:familyId/:recipeId

module.exports = router;
