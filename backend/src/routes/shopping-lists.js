const { Router } = require('express');

const router = Router();

// POST /api/shopping-lists
// GET  /api/shopping-lists/:familyId
// PUT  /api/shopping-lists/:listId
// DELETE /api/shopping-lists/:listId

module.exports = router;
